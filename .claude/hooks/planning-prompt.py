#!/usr/bin/env python3
import json
import sys

# Simple prompt to ask if user wants planning mode
print("Add /plan to your prompt to enable detailed planning and task breakdown", file=sys.stderr)

# Exit successfully to allow the prompt to continue
sys.exit(0)